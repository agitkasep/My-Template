import React from 'react';

const MasalahNavigasi = ({ search }) => {
  return (
    <div className="bg-white p-6 rounded-xl shadow-sm border border-gray-100">
      <h2 className="text-xl font-bold text-gray-800 mb-4">🧭 Navigasi & File</h2>
      <p className="text-gray-500 italic">Halaman Navigasi masih kosong. Siap diisi dengan daftar perintah seperti ls, cd, cp, dll.</p>
      {/* Konten Accordion / Daftar Masalah akan masuk ke sini */}
    </div>
  );
};

export default MasalahNavigasi;
