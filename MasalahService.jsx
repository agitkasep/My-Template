import React from 'react';

const MasalahService = ({ search }) => {
  return (
    <div className="bg-white p-6 rounded-xl shadow-sm border border-gray-100">
      <h2 className="text-xl font-bold text-gray-800 mb-4">⚙️ Manajemen Service</h2>
      <p className="text-gray-500 italic">Halaman Service masih kosong. Siap diisi dengan daftar perintah systemctl (restart, stop, enable).</p>
      {/* Konten Accordion / Daftar Masalah akan masuk ke sini */}
    </div>
  );
};

export default MasalahService;
